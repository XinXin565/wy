# 卡密验证系统后台控制面板整体界面交互与 UI 优化计划书

## 一、项目目标

将当前 MVP 后台升级为结构清晰、操作稳定、适合日常运营使用的企业级管理控制台。

核心目标：

- 统一页面结构、导航、表格、弹窗、状态和反馈规范。
- 让卡密生成、筛选、批量操作、导出、封禁等高频操作可快速完成。
- 消除旧模板、内联脚本和多套渲染函数互相覆盖造成的交互问题。
- 保持现有 PHP + SQLite + Layui 技术栈，先完成前端和交互治理，再为 Laravel/PostgreSQL 演进预留接口。

## 二、当前问题判断

### 1. 前端结构问题

- `index.php` 中包含大段内联 HTML、CSS 和 JavaScript，维护成本高。
- 旧版页面逻辑和 Layui 页面同时存在。
- `admin_fix.js` 多次重写 `renderLicenses()`、`openCreate()`，执行顺序会影响最终行为。
- `/admin` 与旧 `/admin/licenses` 页面存在重复实现，用户容易进入不一致的页面。

### 2. 交互问题

- 卡密列表筛选条件不统一，部分控件只筛选单个条件。
- 批量操作缺少明确的选择状态、影响范围和完成反馈。
- 刷新、返回、分页、筛选状态的保留规则不完整。
- 异步请求中的加载中、失败、重试和空数据状态不统一。

### 3. 信息展示问题

- 卡密、到期策略、到期时间、状态之间的层级不够清晰。
- 日期格式、状态中文化、设备唯一码等字段需要统一展示规范。
- 总览页偏向静态数据展示，缺少可直接进入处理流程的快捷入口。

## 三、目标信息架构

### 一级导航

1. 总览
2. 卡密管理
3. 设备管理
4. 审计日志
5. 系统设置

### 总览页

- 授权总数、已激活、未激活、即将到期、已封禁、已撤销。
- 最近生成卡密。
- 最近激活和心跳异常。
- 即将到期列表。
- 快捷操作：生成卡密、导出卡密、查看封禁卡、查看异常日志。

### 卡密管理页

表格固定列：

1. 卡密
2. 产品
3. 状态
4. 设备上限
5. 到期策略
6. 到期时间
7. 创建时间
8. 操作

筛选区支持同时筛选：

- 卡密/产品关键词。
- 产品。
- 状态。
- 设备上限。
- 到期策略。
- 到期时间范围。

操作包括：复制卡密、编辑、封禁/解封、删除、批量删除、导出 CSV、导出 TXT。

### 设备管理页

- 设备唯一码。
- 绑定卡密。
- 产品。
- 设备状态。
- 首次绑定时间。
- 最近心跳时间。
- 手动解绑和查看验证记录。

### 审计日志页

- 时间范围、动作、结果、卡密、设备唯一码筛选。
- 支持分页、详情抽屉和日志导出。
- 失败原因使用中文业务描述，保留原始错误码供详情查看。

## 四、核心交互方案

### 1. 导航与页面状态

- 使用单页导航和 URL hash。
- 刷新后保留当前页面、筛选条件、关键词和分页位置。
- 浏览器前进/后退可以恢复页面状态。
- 当前导航高亮必须与实际页面一致。
- 删除或生成完成后停留在当前页面，不跳回总览。

### 2. 卡密筛选

- 所有筛选条件使用 AND 逻辑同时生效。
- 筛选条件变化后自动刷新表格，不刷新整个页面。
- 显示“当前筛选条件”和“清空筛选”按钮。
- 无匹配数据时显示筛选条件摘要和清空入口。
- 产品、设备上限、到期策略选项从实际数据动态生成。

### 3. 批量操作

- 表头提供全选和反选。
- 未选择数据时批量按钮禁用。
- 批量删除、封禁、解封必须使用二次确认弹窗。
- 确认框显示影响数量，例如“确定删除 25 张卡密？”。
- 完成后显示成功数量、失败数量和失败原因。

### 4. 生成卡密

- 弹窗分为“基础设置”和“高级设置”。
- 基础设置：产品、数量、设备上限、到期策略。
- 高级设置：长度、前缀、后缀、字符集。
- 自定义到期策略选择日期和时间。
- 提交时显示加载状态，禁止重复点击。
- 成功后使用 Layui 消息提示，不使用浏览器原生 alert。
- 生成后停留在卡密管理页，并自动刷新列表。

### 5. 危险操作反馈

- 封禁、删除、撤销使用红色危险操作样式。
- 成功、失败、警告、网络错误使用统一消息组件。
- 所有异步操作至少具备：加载中、成功、失败、重试四种状态。

## 五、UI 设计规范

### 视觉方向

- 企业后台风格：浅色内容区、深色侧边栏、蓝色主操作色。
- 页面区块使用统一 8px 圆角，减少装饰性卡片堆叠。
- 表格优先保证信息密度和可扫描性。
- 主按钮只保留一个视觉重点，危险操作使用红色。

### 布局规范

- 侧边栏宽度 220-240px，支持收起。
- 顶部栏显示当前位置、当前管理员和刷新入口。
- 内容区最大宽度 1440px，窄屏自动横向滚动表格。
- 表格操作列固定在右侧，避免横向滚动后丢失操作入口。

### 组件规范

- 按钮、下拉框、输入框、分页、弹窗、消息、标签全部使用 Layui 组件。
- 状态标签统一中文：未激活、已激活、已封禁、已撤销、已到期。
- 时间统一显示 `YYYY-MM-DD HH:mm:ss`。
- 卡密默认等宽字体显示，复制按钮使用图标并提供悬浮提示。
- 图标统一使用本地可用图标字体或 Layui 图标，禁止出现缺失图标。

## 六、技术实施方案

### 阶段 1：前端结构治理

- 保留一个 `/admin` 入口，旧 `/admin/licenses` 统一跳转或移除。
- 将内联模板、样式、脚本拆分为独立文件。
- 删除重复的 `renderLicenses()`、`openCreate()` 和旧页面脚本。
- 建立单一数据模型和单一渲染入口。

### 阶段 2：组件和状态统一

- 封装表格、筛选栏、分页、弹窗、消息、确认框。
- 建立状态中文映射、时间格式化和金额/数量格式化工具。
- 统一 API 请求错误处理和加载状态。

### 阶段 3：卡密管理升级

- 实现联合筛选、分页、排序和筛选条件持久化。
- 实现批量删除、批量封禁、批量解封。
- 完善 CSV/TXT 导出，导出内容与当前筛选条件一致。
- 增加卡密详情抽屉和操作审计。

### 阶段 4：总览和运营能力

- 增加到期预警、异常心跳、封禁统计。
- 增加快捷操作和最近活动流。
- 增加时间范围和产品维度统计。

### 阶段 5：质量与扩展

- 使用 Playwright 验证桌面和移动端布局。
- 覆盖生成、筛选、批量删除、导出、封禁、刷新留存等流程。
- 为 Laravel API、PostgreSQL、Redis 分页和权限系统预留接口层。

## 七、验收标准

- `/admin` 所有导航均为单页切换，刷新后页面和导航高亮一致。
- 卡密列表严格显示 8 个指定字段。
- 产品、状态、设备上限、到期策略四个筛选可同时生效。
- 筛选、分页、关键词和页面刷新不会丢失状态。
- 生成卡密后不跳转总览，成功提示自动消失。
- 批量操作有数量确认、加载状态和结果反馈。
- CSV/TXT 导出字段与列表字段一致，中文无乱码。
- 日期统一为 `YYYY-MM-DD HH:mm:ss`，不出现 ISO 字母、毫秒和时区后缀。
- 桌面 1920px、1366px、平板和手机宽度下无关键内容重叠。
- 浏览器控制台无 JavaScript 运行错误。

## 八、实施优先级

### P0：必须先做

- 删除重复渲染逻辑。
- 固定卡密列表字段和联合筛选。
- 统一生成、删除、封禁、导出反馈。
- 修复刷新后页面状态和导航高亮。

### P1：第二阶段

- 分页、排序、批量封禁/解封。
- 卡密详情抽屉。
- 总览到期预警和异常统计。

### P2：后续大型项目能力

- 管理员登录、角色权限、操作授权。
- PostgreSQL、Redis、队列和审计归档。
- 多产品、多租户、API 密钥和配置中心。
- 主题切换、国际化、报表和告警通知。

## 九、交付物

- 后台页面信息架构和组件规范。
- 独立的 Layui 页面模板、样式和脚本。
- API 请求与错误码映射表。
- Playwright 页面流程测试。
- 后台操作手册和部署说明。

