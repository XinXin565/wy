# 产品级公告、版本号与云配置下发实施计划书

## 1. 目标

为每个产品建立独立的云端配置，使客户端启动时先确认产品存在，再读取该产品的：

- 公告内容
- 当前版本号
- 最低支持版本
- 更新地址和更新说明
- 产品级特殊加密配置

客户端只有在产品校验通过后，才进入卡密激活、登录和心跳流程。

## 2. 适用范围

本期继续使用现有 PHP + SQLite + Layui 管理后台，并保持 v3 客户端协议兼容。

配置按 `product_code` 隔离，产品 A 的公告、版本和加密配置不会影响产品 B。

## 3. 产品配置模型

### 3.1 产品基础字段

继续使用现有 `products` 表：

| 字段 | 说明 |
|---|---|
| `id` | 产品 ID |
| `code` | 产品唯一编号，客户端写入此值 |
| `name` | 产品显示名称 |
| `status` | `active` / `disabled` |

### 3.2 新增产品配置表

新增 `product_configs`：

| 字段 | 说明 |
|---|---|
| `id` | 配置 ID |
| `product_id` | 关联产品 ID |
| `product_code` | 冗余业务键，便于查询和迁移 |
| `announcement` | 公告正文 |
| `announcement_enabled` | 是否显示公告 |
| `version` | 当前最新版本号 |
| `min_version` | 最低允许运行版本 |
| `update_url` | 更新地址 |
| `update_notes` | 更新说明 |
| `crypto_profile` | 产品级加密配置版本标识 |
| `crypto_config` | 服务端加密配置 JSON，禁止存放客户端私钥 |
| `config_version` | 配置递增版本号 |
| `updated_at` | 更新时间 |
| `updated_by` | 最后修改管理员 |

约束：`product_id` 唯一，`product_code` 唯一，配置删除跟随产品删除或禁止删除有依赖的产品。

## 4. 后台功能

### 4.1 产品管理新增配置入口

产品列表每行增加“配置”按钮，打开 Layui 弹层，分为三个面板：

1. **公告设置**
   - 开关
   - 公告标题
   - 公告正文
   - 公告发布时间
   - 是否强制阅读

2. **版本设置**
   - 当前版本号
   - 最低版本号
   - 更新地址
   - 更新说明
   - 是否强制升级

3. **云加密配置**
   - 配置 profile 名称
   - 密钥版本号
   - 是否启用配置轮换
   - 服务端下发参数
   - 配置生效时间

敏感字段只显示摘要或掩码，后台不回显完整密钥材料。

### 4.2 产品筛选和状态

- 只允许对启用产品进行客户端下发。
- 停用产品仍保留历史卡密和历史配置查询能力。
- 产品列表增加：当前版本、最低版本、公告状态、配置版本、更新时间。

### 4.3 审计记录

新增后台动作：

- `product.config.create`
- `product.config.update`
- `product.config.publish`
- `product.config.rollback`
- `product.config.rotate`

审计日志记录产品、动作、结果、原因、管理员、IP、配置版本和请求 ID。

## 5. 客户端启动流程

客户端增加产品变量，例如：

```cpp
constexpr char PRODUCT_CODE[] = "debug";
constexpr char CLIENT_VERSION[] = "3.0.0";
```

启动顺序固定为：

1. 读取编译期 `PRODUCT_CODE`。
2. 通过 v3 加密接口请求产品配置。
3. 服务端校验产品是否存在且状态为 `active`。
4. 产品不存在：显示“产品不存在或已停用”，停止后续流程。
5. 产品存在：校验客户端版本是否低于 `min_version`。
6. 低于最低版本：显示升级提示；强制升级时禁止进入卡密验证。
7. 读取公告、版本和云配置。
8. 公告开启时显示公告窗口或状态提示。
9. 使用返回的 `config_version` 和 `crypto_profile` 初始化本次 v3 通讯参数。
10. 产品校验通过后，才调用卡密激活接口。

客户端不允许通过用户输入修改产品编号，产品编号由每个产品的客户端构建配置决定。

## 6. v3 接口设计

### 6.1 产品配置接口

```text
POST /api/v3/products/bootstrap
```

请求业务字段：

```json
{
  "product_code": "debug",
  "client_version": "3.0.0",
  "device_id": "...",
  "device_name": "...",
  "client_nonce": "..."
}
```

请求继续使用现有 `LK3` 二进制封包，响应继续使用 `LR3` 二进制封包。

成功响应业务字段：

```json
{
  "product": {
    "code": "debug",
    "name": "调试产品",
    "status": "active"
  },
  "announcement": {
    "enabled": true,
    "title": "维护公告",
    "content": "...",
    "force_read": false
  },
  "release": {
    "version": "3.1.0",
    "min_version": "3.0.0",
    "force_update": false,
    "update_url": "...",
    "notes": "..."
  },
  "crypto": {
    "profile": "debug-v3-2026-08",
    "config_version": 12,
    "parameters": {}
  },
  "server_time": "YYYY-MM-DDTHH:mm:ss+00:00"
}
```

失败码：

- `product_not_found`
- `product_disabled`
- `client_version_too_old`
- `product_config_unavailable`
- `invalid_product_request`

### 6.2 后续接口关联

现有接口继续保留 `product_code`：

- `/api/v3/licenses/activate`
- `/api/v3/licenses/heartbeat`
- `/api/v3/licenses/logout`

服务端要求同一会话的产品编号与启动握手产品编号一致，防止跨产品复用卡密会话。

## 7. 云加密配置设计

云下发内容只包含参数和版本标识，不下发服务端私钥或管理密钥。

推荐结构：

```json
{
  "profile": "debug-v3-2026-08",
  "config_version": 12,
  "algorithm": "AES-256-GCM",
  "key_id": "debug-key-12",
  "request_schema": "v3-r2",
  "rotation_at": "2026-09-01T00:00:00Z",
  "parameters": {
    "timestamp_skew": 120,
    "nonce_bytes": 24,
    "heartbeat_seconds": 5
  }
}
```

客户端内置可信的服务端公钥和 profile 白名单；服务端返回的配置需要校验签名、版本和字段范围，禁止任意远程参数直接改变客户端安全边界。

## 8. 数据与缓存策略

- 服务端配置以数据库为准。
- 客户端只缓存最近一次成功配置，并带 `config_version`。
- 启动握手失败时不能把过期缓存当作有效产品授权；仅允许展示离线错误状态。
- 配置更新后递增 `config_version`，客户端下次启动或心跳获取新配置。
- 公告内容设置最大长度，防止异常大文本和脚本注入。

## 9. 权限与校验

- 只有管理员和产品操作员可以修改产品公告、版本和配置。
- 只读角色只能查看脱敏配置。
- 所有写操作使用现有 CSRF、请求 ID 和幂等键。
- 产品编号严格匹配 `[A-Za-z0-9_.-]{1,64}`。
- 版本号使用 SemVer 校验：`major.minor.patch`。
- 公告输出前后端均进行 HTML 转义，禁止执行脚本。

## 10. 客户端交互

启动状态至少包括：

- 正在检查产品
- 产品不存在
- 产品已停用
- 发现新版本
- 当前版本过低
- 公告加载成功
- 正在验证卡密
- 登录成功

产品不存在时不显示“卡密错误”，避免把产品配置错误误报为卡密问题。

## 11. 测试计划

### 服务端接口测试

- 有效产品返回公告、版本和 crypto profile。
- 不存在产品返回 `product_not_found`。
- 停用产品返回 `product_disabled`。
- 当前版本低于最低版本返回 `client_version_too_old`。
- 产品 A 配置不会返回给产品 B。
- 配置版本递增和审计记录正确。
- 重复幂等键返回首次响应。
- v3 封包解密失败、重放 nonce、错误产品编号均被拒绝。

### 后台 Playwright 测试

- 产品配置新增、编辑、发布、回滚。
- 公告开关和强制阅读设置。
- 版本号和最低版本设置。
- crypto profile 配置脱敏显示。
- 不同产品切换后配置内容隔离。
- 刷新页面后产品和配置状态保持。

### C++ 客户端测试

- 修改 `PRODUCT_CODE` 为存在产品，启动进入卡密验证。
- 修改为不存在产品，启动显示产品不存在并停止。
- 停用产品，启动显示产品已停用并停止。
- 版本过低时显示升级提示。
- 公告能够显示。
- 配置更新后重新启动读取新版本。
- 产品握手通过后才发送 activate。

## 12. 实施顺序

### P0：数据和接口

1. 创建 `product_configs` 表和迁移。
2. 增加后台配置 CRUD 和权限。
3. 增加 `/api/v3/products/bootstrap`。
4. 增加产品存在性、状态和版本校验。

### P1：后台界面

1. 产品配置弹层。
2. 公告、版本、云配置三个面板。
3. 配置发布、回滚和审计。
4. 产品列表展示配置摘要。

### P2：客户端

1. 增加 `PRODUCT_CODE` 和 `CLIENT_VERSION` 编译变量。
2. 实现启动 bootstrap 请求。
3. 实现产品不存在、停用、版本过低和公告提示。
4. 将 bootstrap 成功作为 activate 前置条件。
5. 编译并完成 v3 联调。

### P3：验收和运维

1. 完成 API、后台和 C++ 自动化测试。
2. 验证多产品配置隔离。
3. 验证重启、配置更新和回滚。
4. 更新 README、客户端接入示例和发布说明。

## 13. 验收标准

- 每个产品可以独立设置公告、版本号、最低版本和云加密 profile。
- 客户端启动第一步校验产品，产品不存在时不进入卡密流程。
- 产品配置通过 v3 加密接口传输。
- 产品 A 的配置无法被产品 B 获取。
- 后台修改有权限控制、CSRF、幂等和审计记录。
- 服务端重启后配置、公告、版本和审计数据仍存在。
- C++ 客户端可以读取配置并按结果继续或停止运行。
- 所有语法检查、接口测试、Playwright 测试和客户端编译通过。
