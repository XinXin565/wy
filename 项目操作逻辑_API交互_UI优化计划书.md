# 卡密验证系统：操作逻辑、API 交互与 UI 优化计划书

## 1. 目标

将当前可用的 MVP 收敛为单一、可维护、可测试的管理后台和验证 API。重点解决：

- 页面和脚本重复实现导致的状态覆盖。
- 产品、卡密、日志数据来源不一致。
- 实时刷新与筛选、分页、导航状态互相覆盖。
- API 权限、错误响应、幂等和审计边界不完整。
- 管理后台缺少统一加载、失败、空数据和确认反馈。

## 2. 当前问题清单

### P0：必须先处理

1. `index.php`、`admin_layui.php`、`admin_fix.js` 同时包含旧版和新版页面，`renderLicenses`、`renderAudit`、`openCreate` 存在多次重写，执行顺序依赖脚本加载时序。
2. `/admin`、`/admin/licenses` 和旧模板路由并存，旧页面仍可被直接访问，功能和字段不一致。
3. 卡密产品筛选同时使用“卡密历史数据”和“产品目录”重建，实时刷新时会互相覆盖。
4. `/admin/data` 返回结构与 `/admin/audit` 不完全一致，前端必须猜测字段是否存在。
5. 后台 API 目前主要依赖 session，API 密钥角色没有完整落实到每个写操作。
6. 管理员默认密码仍写在页面提示中，首次登录后没有强制修改密码流程。
7. 管理后台写操作缺少 CSRF 防护，批量操作也没有统一请求幂等标识。
8. 审计日志中的动作、来源、产品、卡密和 IP 由前端组合，缺少服务端统一分类字段。

### P1：近期必须完成

1. 卡密列表使用客户端全量数据，数据量增加后筛选、排序和分页会变慢。
2. 产品停用、删除和卡密生成之间没有明确的业务状态规则。
3. 时间字段仍有 ISO 字符串和本地显示混用风险。
4. API 错误格式不统一，部分失败响应没有 `request_id`、字段级错误和可读中文原因。
5. 实时刷新没有版本号或更新时间校验，旧请求可能覆盖新请求。
6. 批量删除、封禁、解封的部分失败结果没有逐条返回。
7. 审计归档后没有归档查询页面、归档统计和恢复保护。

### P2：体验和运营优化

1. 总览缺少按产品、状态、到期区间的统计图表。
2. 产品管理缺少卡密数量、在线设备数量和最近操作摘要。
3. 设备管理缺少按产品、卡密、在线状态筛选和解绑确认详情。
4. 缺少统一主题、密度、表格列显示和移动端折叠策略。
5. 缺少浏览器端自动化回归测试和接口契约测试。

## 3. 目标架构

### 3.1 前端

- `/admin` 作为唯一后台入口，旧 `/admin/licenses` 仅做 301/内部跳转。
- Layui 页面只保留一套模板、一套状态模型、一套渲染入口。
- 按模块拆分：`admin_core.js`、`licenses.js`、`products.js`、`devices.js`、`audit.js`、`settings.js`。
- 所有模块通过统一 `apiRequest()` 请求接口，通过统一 `stateStore` 保存 hash、筛选、分页和排序。

### 3.2 API

- 后台接口统一前缀 `/admin/api/*`。
- 客户端验证接口继续使用 `/api/v1`、`/api/v2`、`/api/v3`，不与后台接口混用。
- 所有 JSON 响应统一：

```json
{
  "ok": true,
  "data": {},
  "error": null,
  "request_id": "..."
}
```

- 错误统一包含 `code`、`message`、`fields`，例如 `product_not_found`、`license_in_use`、`permission_denied`。
- 所有写操作支持 `Idempotency-Key`，重复提交返回第一次结果。

### 3.3 数据

- 产品编号作为稳定业务键，产品名称只作为展示字段。
- 卡密保存 `product_id`，同时保留 `product_code` 作为兼容字段，后续逐步迁移。
- 审计表服务端直接写入 `source`、`product_id`、`product_code`、`license_id`、`device_id`、`ip_address`。
- 时间统一存储 UTC，API 输出标准时间字段，前端统一格式化为 `YYYY-MM-DD HH:mm:ss`。

## 4. 实施阶段

### P0：收敛和修正

1. 建立单一后台入口，旧页面跳转到 `/admin#licenses`。
2. 删除重复模板和重复渲染函数，保留唯一 `renderLicenses()`、`renderAudit()`、`openCreate()`。
3. 建立 `apiRequest()`：统一超时、加载状态、401 跳转、错误提示和 `request_id`。
4. 产品筛选只读取产品目录；产品名称作为显示文本，产品编号作为 value。
5. 实时刷新采用请求序号，只有最新响应可以更新界面。
6. 写操作增加 CSRF token 和请求幂等键。
7. 登录后强制修改默认密码，移除页面中的默认密码提示。

验收：刷新、切换页面、实时更新 10 分钟后，筛选、分页、排序和导航状态不改变；旧路由不会渲染旧页面。

### P1：API 和业务一致性

1. 卡密列表改为服务端分页、筛选和排序。
2. 产品 CRUD 增加状态规则：停用产品不能生成新卡密，但不影响已有卡密验证。
3. 删除产品前由服务端检查关联卡密，返回明确 `product_in_use`。
4. 批量接口返回成功 ID、失败 ID、失败原因和总数。
5. API 密钥角色落实：只读、操作员、管理员分别控制查看、业务写入、系统设置。
6. 审计日志服务端写入来源和产品，不再由前端推断。
7. 增加归档日志查询、归档统计和只读保护。

验收：同一请求重复提交不会重复生成/删除；任意 API 失败都能通过 `request_id` 定位审计记录。

### P2：后台体验

1. 总览增加产品维度统计、到期预警、在线设备趋势和失败原因排行。
2. 产品管理增加关联卡密数量、在线设备数量、最后活动时间。
3. 设备管理增加产品、卡密、在线状态、最后心跳时间筛选。
4. 审计日志增加日期范围、来源、产品、卡密、动作、结果、IP 筛选和导出。
5. 表格支持列显隐、密度切换、固定操作列和响应式横向滚动。
6. 所有弹窗统一使用 Layui，禁止浏览器 `alert`、`prompt`、`confirm`。

验收：桌面 1920px、1366px、平板和手机宽度下无核心字段重叠；所有危险操作均有二次确认和结果明细。

### P3：质量和演进

1. 使用 Playwright 覆盖登录、产品 CRUD、生成卡密、联合筛选、批量操作、导出、审计筛选和刷新保持状态。
2. 使用 PHP 接口测试覆盖激活、重复设备、到期、封禁、心跳、退出和幂等。
3. 增加数据库迁移脚本和备份/恢复脚本。
4. 增加健康检查：数据库、RSA 密钥、存储目录、会话和时间同步。
5. 为 Laravel/PostgreSQL 迁移保留 DTO、分页协议和错误码兼容层。

## 5. API 计划

### 后台

- `GET /admin/api/products`
- `POST /admin/api/products`
- `PATCH /admin/api/products/{id}`
- `DELETE /admin/api/products/{id}`
- `GET /admin/api/licenses?page=&page_size=&product_id=&status=&duration=`
- `POST /admin/api/licenses/batch`
- `POST /admin/api/licenses/batch-status`
- `GET /admin/api/audit?page=&source=&product_id=&license_key=&action=`
- `GET /admin/api/audit/archive`

### 客户端

- 保持 `/api/v1/licenses/activate`
- 保持 `/api/v1/licenses/heartbeat`
- 保持 `/api/v1/licenses/logout`
- 保持 `/api/v1/licenses/verify`
- v2/v3 只作为协议封装层，业务响应使用同一错误码表。

## 6. 测试清单

- 产品新增后立即出现在卡密生成和卡密筛选中。
- 停用产品不能生成新卡密。
- 历史卡密使用已删除/停用产品时，列表和验证结果保持可解释。
- 来源、产品、卡密三个日志筛选可以同时使用，实时刷新后仍保持。
- 无卡密选中时导出当前筛选结果，有选中时只导出选中项。
- 重复点击生成、删除、封禁不会产生重复请求。
- 401、403、409、429、500 均显示统一中文提示和 request_id。
- 客户端心跳停止超过阈值后设备状态自动离线，审计日志记录最后 IP 和原因。

## 7. 交付顺序

1. P0：入口、脚本、API 请求层、筛选和实时刷新收敛。
2. P1：服务端分页、权限、幂等、审计字段和业务规则。
3. P2：报表、设备运营、日志高级筛选和 UI 细节。
4. P3：自动化测试、迁移工具、健康检查和部署文档。

每个阶段完成后必须执行 PHP/JS 语法检查、接口冒烟测试和 Playwright 回归测试，再进入下一阶段。
