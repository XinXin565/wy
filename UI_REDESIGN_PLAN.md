# 卡密验证系统 UI 重设计计划书

## 1. 文档目的

本计划用于审阅后台管理系统和 C++ Forms 客户端的整体 UI 重设计方案。

本次重设计使用现有 `taste-skill` 的反模板设计原则，保留现有业务流程、API 路径和 Layui 技术基础，重点改善视觉统一性、信息层级、交互反馈和桌面端体验。

## 2. 设计定位

### 2.1 后台管理端

- 类型：企业级授权运营控制台
- 用户：管理员、运营人员、审计人员
- 重点：高效扫描、批量操作、状态判断、审计追踪
- 视觉：冷灰蓝底色、半透明玻璃面板、单一青蓝强调色
- 信息密度：中高密度，优先保证表格可读性

### 2.2 C++ 客户端

- 类型：面向最终用户的授权登录 SDK 示例
- 用户：接入验证系统的软件用户
- 重点：产品识别、公告版本、卡密登录、心跳状态
- 视觉：Win11 风格普通窗口、轻量玻璃材质、克制渐变
- 产品配置：
  - 产品编号：`1`
  - 产品名称：`XinGodSKJH`

## 3. 视觉设计原则

### 3.1 液态玻璃实现范围

使用 Web 端玻璃拟态近似效果，不宣称为 Apple 原生 Liquid Glass：

- 半透明背景层
- `backdrop-filter: blur(...)`
- 内侧高光边缘
- 半透明描边
- 柔和的背景阴影
- `prefers-reduced-transparency` 下回退为不透明背景

玻璃效果只用于页面壳层、工具栏、筛选区、弹层和重点状态，不覆盖所有表格单元格，避免降低可读性。

### 3.2 色彩

- 基础背景：冷灰、烟蓝、深石墨
- 主强调色：青蓝色，仅保留一个主交互色
- 成功：低饱和绿色
- 警告：琥珀色
- 错误：低饱和红色
- 渐变只用于背景光、选中态和重点区域
- 不使用大面积紫色霓虹、随机光晕和通用 AI 风格渐变

### 3.3 形状和动效

- 卡片、弹层、输入框统一中等圆角
- 按钮保留清晰边界，不使用纯装饰性胶囊按钮
- 页面切换：150 至 220ms 淡入和位移
- 数据刷新：局部更新，不整页跳转
- 操作反馈：Layui 玻璃通知、内联错误和确认弹层
- 支持减少动画设置

## 4. 后台页面改造范围

### 4.1 全局布局

- 左侧导航改为半透明固定侧栏
- 顶部工具栏改为玻璃材质
- 导航切换保持单页切换，不改变 URL 业务路径
- 刷新后恢复当前页面、筛选条件和导航高亮
- 统一页面背景、标题、工具栏、表格和弹层样式

### 4.2 总览

保留并优化以下内容：

- 授权总数
- 活跃卡密
- 在线设备
- 审计失败数
- 授权趋势
- 最近客户端事件
- 最近后台操作

增加加载、空数据和错误状态，避免接口等待时出现空白区域。

### 4.3 卡密管理

列表字段固定为：

| 卡密 | 产品 | 状态 | 在线状态 | 设备上限 | 到期策略 | 到期时间 | 创建时间 | 操作 |
|---|---|---|---|---|---|---|---|---|

功能要求：

- 产品、状态、设备上限、到期策略同时筛选
- 搜索完整卡密或卡密前缀
- 批量生成、批量删除、批量导出
- 封禁、解封、编辑、删除
- 复制完整卡密
- 选中项导出，未选中时导出当前筛选结果
- 固定列宽，禁止刷新或数据更新后列错位
- 所有确认和结果提示使用 Layui 弹层

### 4.4 产品管理

产品配置分为四个区域：

1. 产品基础信息
2. 公告和版本
3. 加密配置
4. JS 云函数脚本

JS 云函数区域包含：

- 脚本版本
- 启用状态
- 脚本编辑器
- 保存
- 发布
- 测试函数
- 历史版本
- 指定版本回滚
- 当前 SHA-256
- 当前签名状态

测试函数必须使用 Layui 弹层，不能使用浏览器原生 `prompt()`。

测试弹层字段：

- 函数名
- JSON 参数
- 执行按钮
- 结果 JSON
- 错误原因

### 4.5 审计日志

日志分为：

- 后台操作记录
- 客户端操作记录
- JS 云函数执行记录

列表字段：

| 来源 | 产品 | 卡密 | 动作 | 结果 | 原因 | IP | 时间 |
|---|---|---|---|---|---|---|---|

云函数执行审计额外显示：

- 函数名
- 脚本版本
- 执行耗时
- 设备唯一码

筛选条件：

- 来源
- 产品
- 卡密输入
- 函数名
- 结果
- 时间范围

## 5. C++ Forms 客户端改造范围

### 5.1 登录窗口

界面内容：

- 产品标识：`XinGodSKJH`
- 最新版本
- 本地版本
- 公告内容
- 卡密输入框
- 登录按钮
- 连接状态

交互状态：

- 产品不存在
- 产品已停用
- 版本过低
- 公告读取失败
- 卡密为空
- 卡密错误
- 卡密已到期
- 卡密已封禁
- 卡密已撤销
- 网络连接失败

### 5.2 登录成功状态

登录成功后显示：

- 登录成功
- 到期时间
- 产品名称
- 设备唯一码摘要
- 云函数读取状态

随后进入心跳详情窗口，不关闭主授权流程。

### 5.3 心跳详情窗口

显示：

- 在线或离线
- 最近心跳时间
- 心跳间隔：5 秒
- 到期时间
- 网络状态
- 服务端返回状态
- 自动下线原因

状态规则：

- 封禁：显示“卡密已被封禁”
- 到期：显示“卡密已到期”
- 撤销：显示“卡密已被撤销”
- 设备失效：显示“当前设备绑定已失效”
- 连续网络失败：显示“网络连续异常，授权会话已下线”

### 5.4 客户端动效

- 窗体淡入
- 登录按钮加载状态
- 登录成功区域展开
- 心跳状态变化过渡
- 错误状态轻量强调
- 不使用循环光效或装饰性粒子动画

## 6. 技术实施方案

### 6.1 后台

- 保留 PHP + Layui
- 增加统一 CSS 变量
- 整理 `admin_layui.php` 基础样式
- 整理 `admin_fix.js` 的重复轮询和动态注入逻辑
- 统一使用 Layui `layer`、`form`、`table`、`element`
- 不修改既有 API 路径、字段和业务权限

### 6.2 客户端

- 保留 C++/CLI Windows Forms
- 不使用 Qt
- 不使用 WinUI 3
- 新增主题颜色和间距常量
- 登录、心跳、提示和状态区域模块化
- 保留 v3 LK3/LR3 加密传输
- 保留会话、心跳、自动下线和 logout

## 7. 分阶段执行

### P1：视觉基础

- 建立色彩、间距、圆角、阴影变量
- 重做后台壳层、导航、顶部栏
- 增加玻璃效果和透明度回退
- 统一按钮、输入框、弹层样式

### P2：后台页面

- 总览页面
- 卡密管理页面
- 产品管理页面
- 审计日志页面
- 脚本测试、发布、历史和回滚界面

### P3：客户端页面

- 登录窗口
- 公告和版本区域
- 登录成功反馈
- 心跳详情窗口
- 错误和离线状态

### P4：验证和交付

- 浏览器桌面宽度验证
- 浏览器窄窗口验证
- 客户端编译
- v3 登录测试
- 云函数测试
- 心跳封禁和到期测试
- logout 测试
- 动效和透明度回退测试

## 8. 验收标准

- 后台所有页面视觉统一
- 不再出现浏览器原生弹窗
- 页面刷新保留页面、筛选和导航高亮
- 表格字段和数据列始终对应
- 产品编号 `1` 和名称 `XinGodSKJH` 校验一致
- 客户端登录、云函数、心跳、自动下线和退出流程不受 UI 改造影响
- 玻璃效果关闭后仍然清晰可用
- 所有状态和错误中文显示
- 桌面端和窄窗口无重叠、溢出和错位
- C++/CLI 编译无错误
- Node 脚本测试通过
- v3 接口回归测试通过

## 9. 变更边界

本次只调整 UI、交互和前端组织方式，不改变：

- 数据库字段含义
- API URL
- v3 加密协议
- 卡密验证规则
- 设备绑定规则
- 心跳间隔和自动下线规则
- 产品编号和产品名称业务校验逻辑
